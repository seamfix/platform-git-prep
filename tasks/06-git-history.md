# Task 06: Git Log, Blame, and Revert

## Objective
Explore Git history and undoing changes.

## Steps
1. Use `git log` to explore commit history
2. Use `git blame` to investigate a file’s line authorship
3. Use `git revert` to undo a previous commit

## Deliverable
Attach screenshots and explain what each command does.
